@*
    For more information on enabling MVC for empty projects, visit http://go.microsoft.com/fwlink/?LinkID=397860
*@

<nav class="navbar navbar-expand-lg navbar-light bg-light" style="font-size:22px;font-weight:bold;">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbardrop" data-toggle="dropdown">
                    <i class="fas fa-user-circle"></i>
                    @ViewBag.GuardianName
                </a>
                <div class="dropdown-menu">
                    <a asp-controller="Guardian" asp-action="Dashboard" class="dropdown-item">Profile</a>
                    <a asp-controller="Guardian" asp-action="Logout" class="dropdown-item">Logout</a>
                </div>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbardrop" data-toggle="dropdown">
                    Student List
                </a>
                <div class="dropdown-menu">

                    @foreach (var item in ViewBag.StuName)
                    {
                        <a asp-controller="Guardian" asp-action="StudentInfo" asp-route-StuId="@item.Id" class="dropdown-item">@item.FirstName &nbsp; @item.LastName</a>
                    }
                </div>

            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbardrop" data-toggle="dropdown">
                    Message
                </a>
                <div class="dropdown-menu">
                    <a asp-controller="Guardian" asp-action="Inbox" class="dropdown-item">Inbox</a>
                    <a asp-controller="Guardian" asp-action="ShowSentMessage" class="dropdown-item">Sent</a>
                </div>
            </li>
        </ul>
    </div>
</nav>