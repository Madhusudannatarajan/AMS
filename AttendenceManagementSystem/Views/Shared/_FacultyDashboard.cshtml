@*
    For more information on enabling MVC for empty projects, visit http://go.microsoft.com/fwlink/?LinkID=397860
*@

    <nav class="navbar navbar-expand-lg navbar-light bg-light" style="font-size:22px;font-weight:bold;">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbardrop" data-toggle="dropdown">
                       @ViewBag.FacultyName
                    </a>
                    <div class="dropdown-menu bg-info">
                        <a asp-controller="Faculty" asp-action="Dashboard" class="dropdown-item">Profile</a>
                        <a asp-controller="Faculty" asp-action="Dashboard" class="dropdown-item">Taken Courses</a>
                        <a asp-controller="Faculty" asp-action="Logout" class="dropdown-item">Logout</a>
                    </div>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbardrop" data-toggle="dropdown">
                        Student List
                    </a>
                    <div class="dropdown-menu bg-info">
                        @foreach (var i in ViewBag.c)
                        {
                            <a asp-controller="Faculty" asp-action="StudentList" asp-route-Id="@i.Id" class="dropdown-item">@i.Classname</a>
                        }
                    </div>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbardrop" data-toggle="dropdown">
                        Message
                    </a>
                    <div class="dropdown-menu bg-info">
                        <a asp-controller="Faculty" asp-action="Inbox" class="dropdown-item">Inbox</a>
                        <a asp-controller="Faculty" asp-action="ShowSentMessage" class="dropdown-item">Sent</a>
                    </div>
                </li>
            </ul>
        </div>
    </nav>